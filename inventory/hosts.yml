---
all:
  hosts:
    first_host:
      ansible_host: localhost
      ansible_connection: local
    second_host:
      ansible_host: localhost
      ansible_connection: local
    third_host:
      ansible_host: localhost
      ansible_connection: local
    fourth_host:
      ansible_host: localhost
      ansible_connection: local
    fifth_host:
      ansible_host: localhost
      ansible_connection: local
    eighth_host:
      ansible_host: localhost
      ansible_connection: local
right_hosts:
  hosts:
    first_host:
    second_host:
    fourth_host:
wrong_hosts:
   hosts:
      third_host:
      fifth_host:
      eighth_host:
         